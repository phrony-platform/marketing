/** Short descriptions for SDK language cards on /docs/sdks (nav group name → blurb). */
export const sdkLanguageDescriptions: Record<string, string> = {
  TypeScript:
    '@phrony/sdk for Node.js 18+ — install, connect, run agents, stream sessions, and register workers.',
};
