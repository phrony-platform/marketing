/** Short descriptions for TypeScript SDK doc cards (href → blurb). */
export const typescriptSdkPageDescriptions: Record<string, string> = {
  '/docs/sdks/typescript/install': 'Add @phrony/sdk and choose import paths.',
  '/docs/sdks/typescript/connect':
    'Dial a runtime with RuntimeClient, Phrony, TLS, health checks, and address resolution.',
  '/docs/sdks/typescript/run':
    'Phrony facade and PhronyAgent — run agents with wait or fire-and-forget.',
  '/docs/sdks/typescript/interactive-session':
    'Stream sessions, handle events, user messages, and tool approvals.',
  '/docs/sdks/typescript/worker':
    'Register tool handlers on the Work stream with Worker and WorkStream.',
  '/docs/sdks/typescript/runtime-client':
    'Unary RPCs for publish, deploy, sessions, approvals, and catalog APIs.',
  '/docs/sdks/typescript/utilities':
    'JSON bytes helpers, agent ref parsing, errors, constants, and proto types.',
};
